/**
 * @file Zenscript grammar for tree-sitter
 * @author Mike Rivnak
 * @license MIT
 */

/// <reference types="tree-sitter-cli/dsl" />
// @ts-check

module.exports = grammar({
  name: "zenscript",

  rules: {
    // NOTE: add the actual grammar rules
    changeme: $ => "",
  }
});
